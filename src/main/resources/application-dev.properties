# dev profile — no overrides needed.
# Game rules (default.duration, optional.durations) come from config.properties (shared base).
# MongoDB is configured via environment variables in application.properties.