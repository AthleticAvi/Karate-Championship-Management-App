# dev profile - active by default via spring.profiles.default in application.properties.
#
# Deliberately empty. Nothing about local development differs from the shared base yet:
# the game.* values are rules rather than environment settings, and MongoDB already comes
# from MONGO_HOST / MONGO_PORT / MONGO_DB with local defaults. Inventing a difference here
# just to fill the file would duplicate a key and let the two copies drift.
#
# Add a property here only when it genuinely differs from application.properties.
